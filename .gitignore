# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build
dist
dist-ssr
build
*.local

# Vite / tooling caches
.vite
.cache
.turbo
*.tsbuildinfo

# Tests & coverage
coverage
.nyc_output

# Environment — ne rien versionner (clés, tokens, URLs projet réelles)
.env
.env.*
.env.local
.env.*.local
backend/.env
ugc-tracking-service/.env
**/.env
render-env-values.local.txt

# Anciens exemples retirés du dépôt ; ne pas re-committer par erreur
.env.example
**/.env.example

# SQL one-shot Vault / secrets locaux — ne jamais committer
**/*.vault.local.sql

# Supabase CLI (artefacts & secrets locaux — pas les migrations)
.branches
.temp
supabase/.temp/
supabase/.branches/

# Deploy / hosting
.vercel
.netlify

# IDE
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
.DS_Store
Thumbs.db

# Debug & misc
*.pem
*.orig

# Fichier de licence à la racine : non versionné pour l’instant (republier : retirer cette ligne puis git add LICENSE)
/LICENSE
